[ VO ]
변수, 생성자, getter/setter

[ mybatis.Spring.xml ]
<typeAliases>
	<typeAlias alias="이름" type="VO주소"/>
</typeAliases>

[ DAO ]
메서드명 선언 - Mapper.xml에 있는 구성요소와 연동
ex) public ArrayList<Emp> empList(Emp sch);

[ Service ]
DAO호출
DAO에서 설정한 메서드 구체화
return dao.메서드명;

[ XXXMapper.xml ]
<mapper namespace="DAO주소">
	<resultMap type="alias이름" id="resultMap이름"/>
	<select id = "DAO 메서드명" resultMap = "resultMap id" parameterType="resultMap type">
		SQL문
	</select>
</mapper>

==> DAO + Mapper = 객체

[ Controller ]
Serveice 호출
@RequestMapping("/aaa.do")
return "view단(JSP파일)";

[ View(jsp) ]
value에 DAO에서 설정한 매개변수?객체? 사용(ex - Emp sch로 했으면 ${sch.ename})
<c:forEach var="설정할이름" items="${mapper select에서 설정한 id}">